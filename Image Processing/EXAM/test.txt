imgThresh = round((np.mean(magnitude)*109.75382) - 256.583)

if imgThresh > 200:
    imgThresh = 200

if imgThresh < 50:
    imgThresh = 50

imgThresh, mask = cv2.threshold(magnitude, thresh = imgThresh, maxval = 255, type = cv2.THRESH_BINARY)

plt.imshow(mask, cmap="gray")